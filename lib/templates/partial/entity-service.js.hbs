/**
 * {{name}} service
 */
const { info, error } = require('./log');

/**
 * Finds a {{name}} by its ID.
 *
 * @param  {string} id Unique ID
 * @param  {object} db Database connection
 * @return {object}    The {{name}}
 */
exports.byId = async function(id, db) {
    // Find and return {{name}}
};

/**
 * Creates a new {{name}}
 *
 * @param  {object} the{{name}} The {{name}} to be created
 * @param  {object} db Database connection
 * @return {object}    The new {{name}}
 */
exports.create = async function(the{{name}}, db) {
    // Create new {{name}} and return it
};

/**
 * Removes a {{name}}
 *
 * @param  {string} id ID of the {{name}} to be removed
 * @param  {object} db Database connection
 */
exports.remove = async function(id, db) {
    // Remove {{name}} with the id
};
